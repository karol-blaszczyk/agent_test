# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# HTTP requests for weather APIs
requests>=2.28.0

# Date and time handling
python-dateutil>=2.8.0

# Configuration and environment variables
python-dotenv>=0.19.0

# Data handling and validation
pydantic>=1.10.0

# Optional: Database support (if storing weather data)
sqlalchemy>=1.4.0

# Optional: Async support (if using async weather APIs)
aiohttp>=3.8.0
aiofiles>=0.8.0

# Optional: Logging enhancements
structlog>=22.0.0

# Optional: CLI support (if making a command-line weather logger)
click>=8.0.0

# Optional: Scheduling (if running periodic weather logging)
schedule>=1.2.0

# Development dependencies (optional)
black>=22.0.0
flake8>=5.0.0
mypy>=0.991